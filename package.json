{
  "name": "passage",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8030 --bind 127.0.0.1 --directory app",
    "check": "node --check app/model.js && node --check app/storage.js && node --check app/main.js"
  }
}
